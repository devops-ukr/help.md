{
    "Command": "docker plugin enable",
    "Description": "Enable a plugin",
    "Usage": "\tdocker plugin enable [OPTIONS] PLUGIN",
    "options": {
        "indent": 21,
        "data": [
            {
                "cmd": "--timeout int",
                "dcr": " HTTP client timeout (in seconds) (default 30)"
            }
        ]
    }
}